# CHANGELOG

원본 대비 변경 사항 정리.

## 맵 데이터 구조

**원본:** `js/level_new.js`에 `LEVELS_DATA` 배열로 30개 레벨이 하드코딩. `map`은 문자열 배열(`["행1", "행2", ...]`) 하나로, 2D 단층 그리드.

**변경:**
- `levels/level_01.json` ~ `level_30.json` — 레벨 하나당 파일 하나로 분리. `levels/manifest.json`에 로드할 파일 목록 명시
- `js/level_new.js`에 `loadAllLevels()` 추가 — manifest를 fetch한 뒤 각 레벨 JSON을 fetch해서 `LEVELS_DATA`를 채움(id 순 정렬). `game.js`의 `DOMContentLoaded`가 `await loadAllLevels()` 후 `new Game()` 생성 (비동기 초기화로 전환)
- `map`을 한 겹 더 감싸 `map[z][y][x]` 3D 구조로 확장 — `map[z]`가 층(레이어), `map[z][y]`가 그 층의 행, `map[z][y][x]`가 타일 문자. 기존 30개 레벨도 전부 `map: [ [...기존 행들...] ]` (1층짜리)로 마이그레이션해 형식 통일
- `Level` 클래스(`level_new.js`)가 3D grid(`grid[z][y][x]`)를 다루도록 전면 개조: `getTile(x,y,z)`, `playerStart`/`portalPos`에 `z` 추가, `consumeItem`/`openDoor`/`triggerRubberBounce`도 z 포함 키 사용
- 시점(뷰) 슬라이싱 지원 추가: `viewAxis`(`'z'` 기본 / `'x'`), `setView()`, `getViewDimensions()`, `viewToGrid()` — Z 고정 X-Y 평면(기존 화면)과 X 고정 Z-Y 평면 두 가지 시점을 자를 수 있음. 단, 실제 시점 전환 UI/입력/이동 로직 연동은 아직 미구현(데이터 구조만 준비된 상태)
- `game.js`, `player.js`에서 `level.grid[y][x]` 식으로 그리드를 직접 참조하던 곳들을 전부 `level.getTile(x, y)` 호출로 교체 (3D 구조에서 안전하게 동작하도록)
- fetch에 `cache: 'no-store'` 지정 — 레벨 JSON이 브라우저 캐시에 걸려 예전 데이터로 로드되는 문제 방지

**레벨 추가 방법:** 새 `level_XX.json` 생성 + `manifest.json`에 파일명 추가. 자동 디렉토리 스캔은 지원 안 함(정적 파일 서버 구조상 불가), manifest 수동 관리.

## 아이템 동작

### 🧲 자석 (MAGNET)

**원본:** 이동 방향의 반대편에 금속벽이 있으면, 그 벽 앞 3칸 지점까지 자동으로 당겨져 이동.

**변경:** 이동 중 지나가는 각 칸의 옆(진행 방향 기준 좌/우)에 금속벽이 있으면 그 칸에서 즉시 정지. 정지 시 기존 금속벽 충돌 이펙트(파티클/사운드/화면흔들림)를 그대로 재사용. 정지가 확정되는 순간 상태가 `NORMAL`로 리셋됨(1회성 소모).

### ❄️ 얼음 (ICE)

**원본:** 벽에 부딪히면 오른쪽 우선(막히면 왼쪽)으로 자동 90도 꺾여서, 다시 막힐 때까지 그 방향으로 쭉 미끄러짐.

**변경:**
- 벽에 부딪히면 이펙트 없이 조용히 멈추고 입력 대기 상태(`iceKickWait`)로 전환
- 다음 스와이프가 진행/반대 방향이면 무시하고 계속 대기, 좌/우 방향이면 그쪽으로 **한 칸만** 이동 시도
  - 막혀 있으면 벽 충돌 이펙트만 재생하고 제자리, 대기 해제(턴 종료)
  - 뚫려 있으면 한 칸 이동 + 이동 이펙트, 그 칸의 아이템/전기문도 일반 이동처럼 처리
- 이 "꺾기" 이동은 턴을 소비하지 않음(별도 move count 증가 없음)
- 꺾기가 완료되는 순간 상태가 `NORMAL`로 리셋됨(1회성 소모)

### ⚡ 전기 (ELECTRIC)

변경 없음. 전기문 통과 시 문이 열리고 상태가 `NORMAL`로 리셋되는 기존 동작 유지.

### 공통: 아이템 습득과 효과 적용 시점 분리

**원본/이전 구현:** 이동 경로 중 아이템을 밟으면, 그 즉시 새 상태가 해당 턴의 나머지 이동 판정(자석 정지, 얼음 벽 충돌 시 꺾기 대기, 전기문 통과, 고무벽 바운스 경로의 벽 판정)에도 반영됨.

**변경:** 아이템을 밟은 시각적 효과(색 전환, 파티클, 사운드)는 그 턴에 바로 나타나지만, 새로 얻은 상태의 게임플레이 효과(자석 인력/정지, 얼음 꺾기 대기, 전기문 통과)는 **다음 턴부터** 적용됨. 즉 이동 경로 판정 자체는 턴 시작 시점의 상태(`startState`)를 기준으로 계산.

## 화면 레이아웃

**원본:** `<canvas id="game">` 하나가 `position: fixed`로 전체 화면을 차지. HUD(스테이지명, 상태 아이콘, 이동수, 리스타트 버튼)를 캔버스 안에 직접 그려 넣음(`_renderHUD`).

**변경:**
- `#app-layout` = 상단 `#title-bar`(HTML 요소로 스테이지명/상태/이동수/리스타트 버튼) + `#main-row`(`#game-container`(캔버스) + `#sidebar`(우측, 향후 시점 전환 버튼/미니맵 등을 위한 빈 영역))
- 캔버스 리사이즈 기준이 `window.innerWidth/Height`에서 `#game-container`의 실제 박스 크기로 변경
- 캔버스에 직접 그리던 HUD 렌더링(`_renderHUD`)을 제거하고, `_updateHUD()`가 title-bar의 DOM 텍스트/색상을 갱신하는 방식으로 교체. 리스타트 버튼과 "스테이지명 탭 → 레벨 선택" 동작도 실제 HTML 버튼/클릭 이벤트로 전환
- 타이틀 화면(스와이프/탭 전, 첫 화면)만은 `<body class="at-title">`로 표시해 title-bar/sidebar를 숨기고 게임 캔버스가 다시 전체 화면을 차지하도록 유지 — 기존 타이틀 화면 룩 그대로 보존

## 미검증 사항

- 위 변경들은 코드 정합성(문법 체크, 로직 리뷰, Node 시뮬레이션) 위주로 검증
- 3D 데이터 구조(Z-Y 뷰 슬라이싱)는 데이터 레벨에서만 검증됐고, 실제 시점 전환 UI·입력·이동 로직 연동은 다음 단계로 남아있음
- 다층(Z>1) 실제 레벨 디자인은 아직 없음
