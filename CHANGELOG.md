# CHANGELOG

원본 대비 변경 사항 정리.

## 맵 데이터 구조

**원본:** `js/level_new.js`에 `LEVELS_DATA` 배열로 30개 레벨이 하드코딩. `map`은 문자열 배열(`["행1", "행2", ...]`) 하나로, 2D 단층 그리드.

**변경:**
- `levels/level_01.json` ~ `level_30.json` — 레벨 하나당 파일 하나로 분리. `levels/manifest.json`에 로드할 파일 목록 명시
- `js/level_new.js`에 `loadAllLevels()` 추가 — manifest를 fetch한 뒤 각 레벨 JSON을 fetch해서 `LEVELS_DATA`를 채움(id 순 정렬). `game.js`의 `DOMContentLoaded`가 `await loadAllLevels()` 후 `new Game()` 생성 (비동기 초기화로 전환)
- `map`을 한 겹 더 감싸 `map[z][y][x]` 3D 구조로 확장 — `map[z]`가 층(레이어), `map[z][y]`가 그 층의 행, `map[z][y][x]`가 타일 문자. 기존 30개 레벨도 전부 `map: [ [...기존 행들...] ]` (1층짜리)로 마이그레이션해 형식 통일
- `Level` 클래스(`level_new.js`)가 3D grid(`grid[z][y][x]`)를 다루도록 전면 개조: `getTile(x,y,z)`, `playerStart`/`portalPos`에 `z` 추가, `consumeItem`/`openDoor`/`triggerRubberBounce`도 z 포함 키 사용
- `game.js`, `player.js`에서 `level.grid[y][x]` 식으로 그리드를 직접 참조하던 곳들을 전부 `level.getTile(x, y, z)` 호출로 교체
- fetch에 `cache: 'no-store'` 지정 — 레벨 JSON이 브라우저 캐시에 걸려 예전 데이터로 로드되는 문제 방지

**레벨 추가 방법:** 새 `level_XX.json` 생성 + `manifest.json`에 파일명 추가. 자동 디렉토리 스캔은 지원 안 함(정적 파일 서버 구조상 불가), manifest 수동 관리.

## 3D 이동 — 시점(뷰) 전환과 전체 축 이동

3D 그리드를 두 가지 평면으로 슬라이스해서 볼 수 있고, 실제로 세 축 모두로 플레이어를 움직일 수 있음.

- **X-Y 뷰(기본)**: Z(층) 고정. 상하좌우 스와이프가 각각 Y/X 이동
- **Z-Y 뷰**: X(열) 고정. 자연스러운 배치(가로=Y, 세로=Z)에서 **시계방향으로 90도 회전**해서 표시 — 화면 가로축이 Z(오른쪽으로 갈수록 Z 감소), 세로축이 Y. 상하좌우 스와이프가 각각 Y/Z 이동

**조작:**
- **Tab** — X-Y ↔ Z-Y 뷰 전환. 전환하는 순간의 주인공 위치를 새 뷰의 고정 슬라이스 값으로 세팅(Z-Y로 갈 땐 주인공의 X, X-Y로 돌아올 땐 원래 있던 층 Z). 이후 주인공이 움직여도 뷰는 따라가지 않음(독립적인 카메라 상태)
- **Q / E** — 현재 뷰의 고정 슬라이스 값을 −1/+1 이동(범위 클램프). 이것도 주인공 위치와 무관하게 독립 동작
- 버튼으로도 같은 동작을 트리거할 수 있도록 `Input`에 `onViewToggle`/`onViewShift` 콜백으로 분리해둠(현재는 키보드만 바인딩)
- 이동 애니메이션 중이거나 `PLAYING` 상태가 아닐 때는 시점 조작 무시(`Input.enabled` 게이트를 그대로 공유)
- 전환 시 기존 레벨 로드용 암전 페이드(`transitionAlpha`)를 재사용하되, 시점 전환 전용으로 더 빠른 속도(8)를 썼다가 완료 후 원래 속도(3)로 복원

**층(Z) 이동:** X-Y 뷰에서는 Z가 절대 바뀌지 않음 — Z를 바꾸려면 Tab으로 Z-Y 뷰로 전환한 뒤 좌/우로 슬라이드. `_movePlayer`가 `level.viewAxis`를 참조해 스와이프 방향을 실제 이동 델타(dx/dy/dz)로 재해석(`constants.js`의 `VIEW_DIR_3D` 테이블). 이동 애니메이션도 X/Y와 동일하게 ease-out 보간 적용(Z축만 순간이동하던 버그 수정 — 원인은 `gridToView`가 뷰의 고정축이 아닌 모든 좌표를 반올림해서 애니메이션 중간값을 죽였던 것; 고정축만 반올림하도록 수정).

**`Level` 클래스에 추가된 뷰 관련 API** (`js/level_new.js`):
- `viewAxis`(`'z'`/`'x'`), `viewZ`, `viewX` — 현재 뷰 상태
- `setView(axis, index)`, `shiftView(delta)` — 뷰 전환/슬라이스 이동
- `getViewDimensions()` — 현재 뷰 평면의 (cols, rows)
- `viewToGrid(col, row)` / `gridToView(x, y, z)` — 뷰 평면 좌표 ↔ 3D 그리드 좌표 상호 변환(회전 매핑 포함). `gridToView`는 좌표가 현재 보이는 평면에 없으면 `null` 반환
- `gridToPixelIfVisible(x, y, z)` — 3D 좌표를 현재 뷰 기준 픽셀 좌표로 변환, 안 보이는 평면이면 `null`

**파티클/이펙트 좌표 버그 수정:** 아이템 습득, 벽 충돌, 전기문 통과, 포탈 스월, 이동 트레일, 전기 스파크 등에서 쓰던 `level.gridToPixel(x, y)` 직접 호출을 전부 `gridToPixelIfVisible` 기반 헬퍼(`game.js`의 `_gridToPixel`)로 교체. 이벤트가 발생한 층/열이 현재 보이는 평면이 아니면 파티클 자체를 생성하지 않음(게임 로직/상태 변경 자체는 그대로 진행, 시각 효과만 스킵).

**아이템 효과의 축 독립성:** 자석의 "옆에 금속벽" 판정과 얼음의 "꺾기 방향" 판정이 이동 축이 아니라 현재 뷰 평면의 반대쪽 축을 정확히 가리키도록 계산 — Z축으로 슬라이드 중에도(Z-Y 뷰) 자석이 Y축 옆의 금속벽에 반응해서 멈추고, Y축 슬라이드가 막혔을 때 얼음이 Z축으로 한 칸 꺾일 수 있음(Z-Y 뷰에서 Y-슬라이드가 막힌 경우). 이동 시작 시점의 뷰 축(`viewAxis`)을 캡처해뒀다가 꺾기 판정에 사용.

## 아이템 동작

### 🧲 자석 (MAGNET)

**원본:** 이동 방향의 반대편에 금속벽이 있으면, 그 벽 앞 3칸 지점까지 자동으로 당겨져 이동.

**변경:** 이동 중 지나가는 각 칸의 옆(진행 방향 기준 좌/우, 3D에서는 현재 뷰 평면의 반대쪽 축)에 금속벽이 있으면 그 칸에서 즉시 정지. 정지 시 기존 금속벽 충돌 이펙트(파티클/사운드/화면흔들림)를 그대로 재사용. 정지가 확정되는 순간 상태가 `NORMAL`로 리셋됨(1회성 소모).

### ❄️ 얼음 (ICE)

**원본:** 벽에 부딪히면 오른쪽 우선(막히면 왼쪽)으로 자동 90도 꺾여서, 다시 막힐 때까지 그 방향으로 쭉 미끄러짐.

**변경:**
- 벽에 부딪히면 이펙트 없이 조용히 멈추고 입력 대기 상태(`iceKickWait`)로 전환
- 다음 스와이프가 진행/반대 방향이면 무시하고 계속 대기, 그 외(현재 뷰 평면의 반대쪽 축) 방향이면 그쪽으로 **한 칸만** 이동 시도
  - 막혀 있으면 벽 충돌 이펙트만 재생하고 제자리, 대기 해제(턴 종료) — 이 경우도 얼음은 소비됨
  - 뚫려 있으면 한 칸 이동 + 이동 이펙트, 그 칸의 아이템/전기문도 일반 이동처럼 처리
- 이 "꺾기" 이동은 턴을 소비하지 않음(별도 move count 증가 없음)
- 꺾기가 완료되는 순간(성공/차단 모두) 상태가 `NORMAL`로 리셋됨(1회성 소모)

### ⚡ 전기 (ELECTRIC)

전기문 통과 시 문이 열리고 상태가 `NORMAL`로 리셋되는 기존 동작 유지. 단, 문 타일 자체는 통과 후에도 화면에서 사라지지 않음 — "열림" 판정은 매번 그 순간의 플레이어 상태(`ELECTRIC`인지)로만 결정되고, 한번 통과했다고 해서 이후 무조건 통과되는 영구 개방 상태를 갖지 않음(전기 아이템은 1회성이라 실질적으로도 재사용 불가).

**버그 수정 — 전기 상태가 통과 후에도 해제되지 않던 문제:** 원인은 이동 애니메이션 완료 콜백(`doSecondaryMove`)이 "이동 시작 좌표"를 구하려고 `this.player.gridX/Y/Z`를 읽었는데, 그 시점엔 `Player.update()`가 이미 도착 좌표로 값을 덮어쓴 뒤였음. 그래서 시작-도착 델타가 항상 0으로 계산되어 전기문 통과 스캔 루프 자체가 실행되지 않았고, 상태 리셋 로직도 호출되지 않았음. `_movePlayer` 최상단에서 이동 시작 좌표를 별도 변수로 미리 캡처해두는 방식으로 수정.

### 공통: 아이템 습득과 효과 적용 시점 분리

**원본/이전 구현:** 이동 경로 중 아이템을 밟으면, 그 즉시 새 상태가 해당 턴의 나머지 이동 판정(자석 정지, 얼음 벽 충돌 시 꺾기 대기, 전기문 통과, 고무벽 바운스 경로의 벽 판정)에도 반영됨.

**변경:** 아이템을 밟은 시각적 효과(색 전환, 파티클, 사운드)는 그 턴에 바로 나타나지만, 새로 얻은 상태의 게임플레이 효과(자석 인력/정지, 얼음 꺾기 대기, 전기문 통과)는 **다음 턴부터** 적용됨. 즉 이동 경로 판정 자체는 턴 시작 시점의 상태(`startState`)를 기준으로 계산.

## 화면 레이아웃

**원본:** `<canvas id="game">` 하나가 `position: fixed`로 전체 화면을 차지. HUD(스테이지명, 상태 아이콘, 이동수, 리스타트 버튼)를 캔버스 안에 직접 그려 넣음(`_renderHUD`).

**변경:**
- `#app-layout` = 상단 `#title-bar`(HTML 요소로 스테이지명/상태/이동수/리스타트 버튼) + `#main-row`(`#game-container`(캔버스) + `#sidebar`(우측, 향후 시점 전환 버튼/미니맵 등을 위한 빈 영역))
- 캔버스 리사이즈 기준이 `window.innerWidth/Height`에서 `#game-container`의 실제 박스 크기로 변경
- 캔버스에 직접 그리던 HUD 렌더링(`_renderHUD`)을 제거하고, `_updateHUD()`가 title-bar의 DOM 텍스트/색상을 갱신하는 방식으로 교체. 리스타트 버튼과 "스테이지명 탭 → 레벨 선택" 동작도 실제 HTML 버튼/클릭 이벤트로 전환
- 타이틀 화면(스와이프/탭 전, 첫 화면)만은 `<body class="at-title">`로 표시해 title-bar/sidebar를 숨기고 게임 캔버스가 다시 전체 화면을 차지하도록 유지 — 기존 타이틀 화면 룩 그대로 보존

## 사이드바 방향도와 탑뷰 미니맵

- 비어 있던 `#sidebar`에 맵 방향도와 탑뷰 미니맵을 추가
- 방향도는 레벨의 실제 `X:Y:Z` 비율로 직육면체를 투영하고, 현재 `Tab` 뷰의 X 또는 Z 슬라이스를 강조 표시
- 현재 뷰 이름과 `Q/E`로 이동하는 슬라이스 번호를 실시간 표시
- 탑뷰 미니맵은 실제 맵의 `X:Z` 비율을 왜곡 없이 유지하고 플레이어 위치·최근 이동 방향·포탈 목표를 같은 좌표계에 표시
- `Tab`, `Q/E`, 플레이어 이동, 레벨 로드, 창 크기 변경 시 사이드바 상태를 자동 갱신
- 반응형 화면에서도 사이드바를 숨기지 않고 축소해 핵심 위치 정보를 유지

## 협업 가이드라인 적용

- 저장소 루트에 `AGENTS.md`를 추가해 수정 이유 주석, 추가 위치, 순서 의존성, 레벨 예약·검증, 포맷팅 및 레거시 보존 규칙을 이후 작업에 자동 적용
- 사이드바 연동을 위해 수정한 기존 코드에 `[MODIFIED]`와 구체적인 변경 이유를 기록
- 구현 완료로 더 이상 유효하지 않은 빈 사이드바·미니맵 미구현 TODO를 제거

## 출시용 비주얼 패스

- 게임 전체 아트 방향을 "생체 실험실 + 네온 젤"로 통일하고 민트·시안·청백색·앰버·바이올렛을 기능별 고유 색으로 지정
- 상단 HUD를 브랜드 마크, 코어 상태, 조작 힌트, 이동 수, 재시작 버튼이 분리된 실험실 제어 패널 형태로 개선
- 타이틀 화면을 시설 배경, JELL-E 키비주얼, 시스템 상태 레이블, 시작 인터랙션을 포함한 출시용 화면으로 재구성
- 레벨 선택을 구역 코드와 잠금 상태가 명확한 탐사 카드 그리드로 개선하고 키보드 포커스 및 비활성 상태를 보완
- JELL-E 캐릭터에 반투명 젤 재질, 외곽 림라이트, 바닥 그림자, 눈 하이라이트, 표정과 상태별 실루엣 효과 추가
- 아이템 이모지를 제거하고 자석·얼음·전기를 고유한 벡터 심볼, 코어 광원, 궤도 링으로 직접 렌더링
- 바닥·일반 벽·금속 벽·고무 벽·전기문·포탈을 각 기능이 재질과 실루엣으로 구분되는 타일 아트로 전면 개선
- 파티클을 단순 원에서 빛 꼬리와 코어가 있는 효과로 개선하고 전기 상태의 랜덤 플리커를 결정적 애니메이션으로 교체
- 방향도와 미니맵을 동일한 홀로그램 팔레트로 정리하고 데스크톱·모바일·모션 감소 환경에 대응
- 게임 규칙, TILE id, 레벨 데이터는 변경하지 않았으므로 solver 재검증 대상에서 제외

## 새 레벨

**31~32 (3D 메커니즘 검증용):**
- `level_31` "Vertical Test" — 2개 층짜리 최소 구성, Z축 이동 확인용
- `level_32` "The Awakening (Z-Y)" — 레벨 1을 Z축으로 완전히 회전 재구성(원래 X축 15칸 → 15개 층으로 변환). 전기문 옆에 전기 아이템 추가

**33~37 (아이템 활용 3D 퍼즐, 신규):** 모두 최소 5×5×5 이상 크기. 각 레벨은 필요한 아이템을 제거하면 풀 수 없다는 것을 BFS 시뮬레이션으로 검증함(실제 슬라이딩/자석/얼음/전기 판정 로직을 그대로 재현한 solver 스크립트로 확인).

- `level_33` "Magnet Shaft" (5×5×5) — 자석 아이템 하나로 Z축 샤프트 중간 층에 정확히 멈추는 법을 익히는 입문 레벨. 자석 없이 슬라이드하면 끝층까지 오버슈트해서 막다른 길.
- `level_34` "Electric Shaft" (5×5×5) — 전기 아이템을 먼저 먹어야 Z축 샤프트의 전기문을 통과할 수 있음.
- `level_35` "Ice Cross" (5×6×6) — Z-Y 뷰에서 Y축으로 미끄러지다 벽에 막히면 얼음 꺾기로 Z축으로 정확히 한 칸 이동. 얼음 없이 그냥 밀면 훨씬 먼 곳까지 오버슈트해서 돌아올 방법이 없음(포탈은 슬라이딩 진입 즉시 멈추게 하는 특수 타일이라, 꺾기 목표 지점 너머로 개방된 복도를 둬서 "정확히 한 칸"이라는 얼음의 특성이 실제로 필요하게 설계).
- `level_36` "Magnet Lattice" (9×7×5) — 자석 아이템 두 개를 순서대로 사용: 첫 번째로 X-Y 평면 복도 중간에 멈추고, 두 번째로 Z축 샤프트를 건넘. 둘 중 하나라도 없으면 클리어 불가.
- `level_37` "The Vault" (9×9×7) — 피날레. 전기문 통과 → 자석으로 Z축 샤프트 하강 → 얼음 꺾기로 다른 층으로 건너가는 순서로 세 아이템을 전부 사용해야 포탈 도달. 세 아이템 각각 제거 시 모두 풀이 불가능함을 확인.

## 시점 전환 트랜지션 재설계 (Q/E 줌, Tab 플립)

기존엔 Tab/Q/E 모두 같은 암전 페이드(`transitionAlpha`)를 공유했음. 이제 완전히 분리:

- **Q/E (슬라이스 이동)**: 방향성 줌+크로스페이드. 이전 프레임을 오프스크린 캔버스에 스냅샷해뒀다가, 새 슬라이스와 반대 배율로 확대/축소시키며 교차 페이드
- **Tab (축 전환)**: 카드 뒤집기(`scaleX` 1→0→1). 가장 얇아지는 절반 지점에서 실제 뷰 축을 교체하고, 그 순간 살짝 어둡게 해 입체감 부여
- **배경 분리**: `Level.render()`를 `renderBackground()`(그라디언트+격자, 항상 고정)와 `renderBoard()`(패널+타일, 트랜지션 대상)로 분리. Q/E·Tab 트랜지션은 `renderBoard()` 결과만 스케일/플립하고 배경은 절대 같이 움직이지 않음. Q/E 스냅샷도 보드만 투명 배경에 캡처(`_drawBoardSnapshot`)
- **경계 처리**: `Level.canShiftView(delta)` 추가 — 이미 끝 슬라이스라 Q/E를 눌러도 실제로 안 바뀌면 스냅샷/트랜지션 자체를 시작하지 않음(빈 효과 재생 방지)
- **Tab 롱프레스**: 280ms 이상 누르고 있으면 보드 위 마우스가 가리키는 칸에 하이라이트 테두리 표시(순수 프리뷰, 이동 애니메이션 없음). 뗄 때 그 칸의 좌표가 새 뷰의 고정 슬라이스가 됨(X-Y→Z-Y 전환이면 그 칸의 X, Z-Y→X-Y면 그 칸의 Z). 짧게 누르면 기존처럼 주인공 위치 기준. `Input`에 `onViewTogglePreview` 콜백 추가, Tab 처리를 keydown 즉시 실행에서 keyup 실행(홀드 타이머 포함)으로 변경

## 뚫린 공간 배경 노출

바닥(`.`)이나 아이템 칸에서, E로 이동하는 다음 슬라이스의 같은 좌표도 지나다닐 수 있는 곳(바닥/포탈/아이템)이면 위아래로 이어지는 샤프트로 보고 해당 타일(또는 아이템 밑 바닥 백드롭)을 그리지 않아 게임 배경이 그대로 비쳐 보이게 함(`Level.isOpenShaftCell`). 다음 슬라이스 축은 현재 뷰에 따라 다름(X-Y 뷰는 Z+1, Z-Y 뷰는 X+1). 벽 타일은 이 규칙 대상이 아니라 항상 그대로 렌더링됨 — 게임플레이(이동/충돌)는 그대로 원래 타일로 취급되므로 영향 없음.

## 전기 블록 원상복구

전기문 통과 시 문이 완전히 사라지는(FLOOR로 영구 전환) 방향으로 손댔던 부분을 되돌림 — 전기문 타일은 통과 후에도 화면에 계속 남아있고, 통과 가능 여부는 매번 그 순간 플레이어 상태(`ELECTRIC`)로만 판단.

## 설정: 화면 대칭 · 효과음

사이드바에 설정 카드 추가, `Level.mirror`(`{ z: {h,v}, x: {h,v} }`)로 X-Y/Z-Y 뷰 각각 좌우/상하 독립 반전 지원:

- `viewToGrid`/`gridToView`가 화면 col/row를 회전 매핑 전에 미러 처리 — 타일·파티클·플레이어 렌더링 전부 자동으로 일관되게 반전
- `_moveDelta`가 미러 설정에 따라 해당 축 이동 부호를 반전시켜, 화면이 뒤집혀도 스와이프 방향과 실제로 보이는 이동 방향이 항상 일치(자석/얼음 등 하류 판정은 축만 보고 부호는 안 봐서 영향 없음)
- 효과음 on/off는 기존 `Audio.enabled` 플래그를 사이드바에서 토글
- `localStorage`의 `jelle_settings`에 저장(`_loadSettings`/`_saveSettings`, `_loadProgress`/`_saveProgress`와 동일 패턴)
- 설정 카드는 소제목(라벨)과 토글 목록을 분리하고, 토글들은 가로 한 줄(`settings-toggle-row`)로 배치해 사이드바가 한 화면에 들어오게 함. 방향도/미니맵 카드 크기도 축소

## 탑뷰 미니맵 조정

- 상하 반전 — z=0 층이 아래쪽에 오도록 타일·뷰 하이라이트·목표/플레이어 마커·진행 방향 화살표 전부 뒤집어서 그림
- 벽/통로 색구분(칸별 walkable/wall 스캔) 제거 — 공간 구조를 노출하지 않도록 전체 단일색 타일로만 채우고, 위치 정보는 슬라이스 하이라이트·목표·플레이어 마커로만 표시
