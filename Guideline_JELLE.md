# 코드 협업 가이드라인 (JELL-E: Escape the Lab)

원본 가이드라인을 이 프로젝트(바닐라 JS + Canvas 2D, 클래스 기반, git 저장소)에 맞게 조정한 버전입니다.
모든 AI 에이전트 및 팀원은 아래 규칙을 **절대적으로 준수**해야 합니다.

## 1. 수정 표기 (Modification Marking)

기존 코드를 수정할 때, 단순한 마킹보다는 **변경 이유**를 주석으로 명확히 남깁니다.

**Good Example**:
```javascript
// [MODIFIED] 자석 정지 판정이 이동 축이 아니라 뷰 평면 반대축을 봐야 해서 viewAxis 캡처 추가
const checkAxis = level.viewAxis === 'z' ? 'x' : 'z';
```

**Bad Example**:
```javascript
// 변경됨
const checkAxis = ...
```

> 참고: 지금 `CHANGELOG.md`가 커밋 단위로 "무엇을, 왜" 바꿨는지 잘 정리하고 있습니다. 이 원칙을 코드 주석 레벨까지 그대로 끌어내리면 됩니다.

## 2. 추가 원칙 (Append / Placement Strategy)

원본 규칙("새 함수는 파일 맨 끝에")은 이 코드베이스엔 그대로 적용할 수 없습니다. 새 타일/아이템 하나를 추가하려면 `constants.js`(TILE, CHAR_TO_TILE, isBlocking, TILE_COLORS)와 `level_new.js`(`_renderTile` switch), `game.js`(이동 판정)까지 여러 파일의 객체 리터럴·switch문 **중간**을 동시에 건드려야 하기 때문입니다. 상황별로 나눠서 적용합니다.

### 2-1. 완전히 새로운 독립 함수 / 클래스 / 클래스 메서드
원본 규칙 그대로: **파일(또는 클래스 바디) 맨 끝에 추가**합니다.

### 2-2. 객체 리터럴에 새 항목 추가 (`TILE`, `CHAR_TO_TILE`, `TILE_COLORS`, `STATE`, `ITEM_TO_STATE` 등)
- 항상 **기존 마지막 항목 뒤에** 새 키를 추가합니다(중간 삽입 금지). 두 사람이 각자 끝에 추가하면 git이 자동 병합할 확률이 높아집니다.
- **ID/이름은 작업 시작 전에 예약**합니다. 현재 `TILE`은 0,1,2,3,4,5,10,11,12를 쓰고 **6~9가 비어 있습니다.** 새 타일을 만들기 전 팀 채널이나 `CHANGELOG.md` 상단에 `"6 = FIRE_WALL (담당: OOO)"` 식으로 먼저 기록하고 시작하세요. 같은 번호를 두 사람이 동시에 쓰면 병합 시 조용히 깨집니다.

### 2-3. `switch`문에 새 case 추가 (`_renderTile`, 이동/충돌 판정 등)
- 새 case는 항상 **`default` 바로 위, 기존 마지막 case 뒤**에 추가합니다.
- 기존 case의 로직은 절대 같이 리팩터링하지 않습니다(3번 규칙과 연결).

### 2-4. 진입점 (Python의 `if __name__ == "__main__":`에 대응)
`game.js` 최하단의 다음 블록이 이 프로젝트의 진입점입니다.
```javascript
window.addEventListener('DOMContentLoaded', () => {
    new Game();
});
```
이 블록은 항상 파일 최하단에 유지하고, 초기화 관련 새 로직은 이 블록 **바로 위**에 추가합니다.

### 2-5. `import` / `<script>` 로드 순서
새 `import` 구문은 파일 최상단에 작성합니다. 이 프로젝트는 모듈 번들러 없이 `index.html`의 `<script>` 태그 순서로 의존성을 관리하므로, 새 JS 파일을 추가할 경우 **`index.html`의 스크립트 순서(의존하는 파일보다 먼저 로드되는 파일 뒤)를 반드시 확인**하고 추가 위치를 명시적으로 리뷰 받습니다.

### 2-6. 메커니즘 간 순서 의존성은 반드시 주석으로 명시
새 메커니즘이 다른 메커니즘의 계산 결과에 의존해서 **판정 순서가 바뀌면 결과가 조용히 틀려지는** 경우(예: 불 블록 경로 스캔은 자석 정지 지점이 먼저 계산된 뒤에 그 축소된 경로를 대상으로 해야 함), 그 의존관계를 코드에 명시적으로 남깁니다. 표시해두지 않으면 다른 사람이 무심코 순서를 바꿔서 버그가 조용히 생길 수 있습니다.

```javascript
// [ORDER-DEPENDENT] magnetStopX/Y 계산이 먼저 끝나야 함.
// 철 벽이 불 블록보다 가까우면 자석으로 그 앞에서 멈추므로
// 불 블록 스캔은 최종 정지 지점까지의 축소된 구간만 봐야 함.
const scanEndX = magnetStopX ?? x;
```

### 2-7. 새 이동 메커니즘은 `_movePlayer`에 인라인으로 쌓지 말고 헬퍼로 분리
`game.js`의 `_movePlayer`가 이미 슬라이드/자석/고무 판정을 한 함수 안에서 처리하느라 200줄 넘게 커져 있습니다. 여기에 새 메커니즘(불 블록 등)을 계속 인라인으로 추가하면 여러 사람이 같은 거대 함수를 동시에 편집하게 되어 충돌이 잦아집니다. 새 메커니즘은 `_checkFirePath(startX, startY, endX, endY)`처럼 **독립된 메서드로 분리**하고, `_movePlayer`에서는 그 메서드를 호출만 합니다(호출 위치는 2-6 규칙에 따라 순서를 주석으로 명시).

## 3. 레벨 데이터 규칙 (신규 — 이 프로젝트 전용)

레벨이 `levels/level_XX.json`으로 파일 단위 분리되어 있어 레벨 내용 자체는 충돌 위험이 낮습니다. 다만 다음은 지켜야 합니다.

- 새 레벨 번호(파일명)도 2-2와 동일하게 **작업 전 예약**합니다.
- `manifest.json`에 새 파일명을 추가할 때는 항상 **배열 맨 끝에 추가**합니다.
- 새 아이템/타일이 레벨의 풀이 가능성에 영향을 준다면(예: 특정 아이템 없이는 클리어 불가한 구조), 이전에 적용했던 방식처럼 **solver(BFS) 검증을 거친 뒤 병합**합니다. 검증 결과는 `CHANGELOG.md`에 한 줄로 남깁니다.
- 현재 존재하는 레벨(1~37)은 추후 AI를 통해 다시 제작될 예정이므로, 이 규칙은 "지금 있는 레벨을 보존하기 위함"이 아니라 **앞으로 만들어질 모든 레벨에 동일하게 적용되는 절차**입니다. 레벨 내용이 통째로 교체되더라도 이 규칙 자체는 유지됩니다.

## 4. 포맷팅 제한 (No Global Formatting)

수정하지 않는 영역의 코드는 절대 건드리지 않습니다. 특히 이 프로젝트처럼 여러 사람이 각자 다른 에디터/포매터를 쓰는 경우, 저장 시 자동 포맷팅(들여쓰기 재정렬, 세미콜론 통일 등)이 전체 파일에 걸리지 않도록 **에디터의 "파일 저장 시 자동 포맷" 옵션을 끄고**, 변경한 로직 부분에만 국소적으로 수정을 적용합니다. 이는 `git diff`를 최소화해 리뷰와 병합을 쉽게 하기 위함입니다.

## 5. 레거시 보존 (Legacy Preservation)

삭제 여부가 확실하지 않은 코드는 바로 지우지 말고 주석 처리(comment out)하는 것이 원칙입니다. 다만 이미 git으로 히스토리가 관리되는 프로젝트이므로, 무분별하게 쌓이면 파일이 죽은 코드로 비대해질 수 있습니다. 다음 조건을 붙여서 적용합니다.

- 삭제 여부가 애매한 경우엔 **이유와 함께** 주석 처리합니다: `// [DEPRECATED-TENTATIVE] 자석 정지 반경 계산 - 불 블록과 결합 시 재사용 가능성 있어 보류`
- 한 기능이 완전히 대체되어 더 이상 참고할 필요가 없다고 팀이 이미 합의했다면(예: 얼음의 "좌우 꺾기" 로직 — 불 블록을 끄는 방식으로 완전히 대체하기로 확정됨), 애매하게 주석 처리해두지 말고 **바로 삭제**합니다. 코드는 `CHANGELOG.md`에 "무엇을 왜 없앴는지"만 한 줄 남기면 커밋 히스토리로 언제든 복원 가능합니다. 확정된 폐기를 계속 주석으로 남겨두는 건 오히려 "아직 쓸 수도 있다"는 오해를 줘서 다른 사람이 그 위에 실수로 다시 로직을 쌓을 위험을 만듭니다.

## 6. 체크리스트 (PR/병합 전)

- [ ] 새로 추가한 TILE id / 레벨 번호가 사전에 예약된 것과 일치하는가
- [ ] 여러 파일에 걸친 변경(2-2, 2-3 대상)이 각 파일에서 모두 "마지막 항목 뒤"에 들어갔는가
- [ ] 다른 메커니즘의 계산 결과에 의존하는 순서가 있다면 `[ORDER-DEPENDENT]` 주석으로 남겼는가
- [ ] 새 이동 메커니즘을 `_movePlayer`에 인라인으로 쌓지 않고 별도 메서드로 분리했는가
- [ ] 손대지 않은 코드 영역에 포맷팅 변경이 섞여 있지 않은가
- [ ] 풀이 가능성에 영향을 주는 변경이면 solver 검증 결과를 남겼는가
- [ ] 완전히 대체된 것으로 확정된 로직은 주석 처리 대신 삭제 + CHANGELOG 기록했는가
- [ ] 수정 이유가 주석/커밋 메시지에 남아 있는가
