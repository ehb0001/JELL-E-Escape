# 코드 협업 가이드라인 (JELL-E: Escape the Lab)

이 저장소에서 작업하는 모든 AI 에이전트와 팀원은 아래 규칙을 준수한다.

## 1. 수정 표기

기존 코드를 수정할 때는 단순한 마킹이 아니라 변경 이유를 주석으로 명확히 남긴다.

```javascript
// [MODIFIED] 자석 정지 판정이 이동 축이 아니라 뷰 평면 반대축을 봐야 해서 viewAxis 캡처 추가
const checkAxis = level.viewAxis === 'z' ? 'x' : 'z';
```

`// 변경됨`처럼 이유를 알 수 없는 주석은 사용하지 않는다. `CHANGELOG.md`의 커밋 단위 변경 기록 원칙을 코드 주석에도 적용한다.

## 2. 추가 및 배치 원칙

### 2-1. 완전히 새로운 독립 함수, 클래스, 클래스 메서드

파일 또는 클래스 바디의 맨 끝에 추가한다.

### 2-2. 객체 리터럴에 새 항목 추가

`TILE`, `CHAR_TO_TILE`, `TILE_COLORS`, `STATE`, `ITEM_TO_STATE` 등에 새 키를 추가할 때는 항상 기존 마지막 항목 뒤에 배치한다.

ID나 이름은 작업 전에 팀 채널 또는 `CHANGELOG.md` 상단에서 예약한다. 현재 `TILE`은 `0, 1, 2, 3, 4, 5, 10, 11, 12`를 사용하며 `6~9`가 비어 있다. 예: `6 = FIRE_WALL (담당: OOO)`.

### 2-3. switch문에 새 case 추가

`_renderTile`, 이동 및 충돌 판정 등의 새 case는 `default` 바로 위, 기존 마지막 case 뒤에 추가한다. 기존 case를 함께 리팩터링하지 않는다.

### 2-4. 진입점

`game.js` 최하단의 `DOMContentLoaded` 초기화 블록은 항상 파일 맨 아래에 유지한다. 초기화 관련 새 로직은 이 블록 바로 위에 추가한다.

### 2-5. import 및 script 로드 순서

새 import는 파일 최상단에 작성한다. 이 프로젝트는 모듈 번들러 없이 `index.html`의 `<script>` 순서로 의존성을 관리하므로 새 JS 파일은 의존 대상 뒤, 자신을 사용하는 파일 앞에 배치하고 그 이유를 리뷰 가능하게 남긴다.

### 2-6. 메커니즘 간 순서 의존성

다른 메커니즘의 계산 결과에 의존하여 판정 순서가 바뀌면 결과가 달라지는 로직에는 `[ORDER-DEPENDENT]` 주석으로 이유와 선행 조건을 명시한다.

```javascript
// [ORDER-DEPENDENT] magnetStopX/Y 계산이 먼저 끝나야 함.
// 철 벽이 불 블록보다 가까우면 자석으로 그 앞에서 멈추므로
// 불 블록 스캔은 최종 정지 지점까지의 축소된 구간만 봐야 함.
const scanEndX = magnetStopX ?? x;
```

### 2-7. 새 이동 메커니즘

새 이동 메커니즘을 `_movePlayer`에 인라인으로 추가하지 않는다. `_checkFirePath(startX, startY, endX, endY)` 같은 독립 메서드로 분리하고 `_movePlayer`에서는 호출만 한다. 호출 순서에 의존성이 있으면 2-6 규칙에 따라 주석을 남긴다.

## 3. 레벨 데이터 규칙

- 새 레벨 번호와 파일명은 작업 전에 예약한다.
- `manifest.json`의 새 파일명은 항상 배열 맨 끝에 추가한다.
- 새 아이템이나 타일이 풀이 가능성에 영향을 주면 solver(BFS) 검증 후 결과를 `CHANGELOG.md`에 한 줄로 기록한다.
- 현재 레벨이 교체되더라도 이 절차는 앞으로 만들어지는 모든 레벨에 동일하게 적용한다.

## 4. 전역 포맷팅 금지

수정하지 않는 영역의 코드는 건드리지 않는다. 저장 시 자동 포맷팅으로 파일 전체의 들여쓰기, 세미콜론, 줄바꿈 등이 바뀌지 않도록 하고 변경한 로직만 국소적으로 수정한다.

## 5. 레거시 보존

- 삭제 여부가 애매한 코드는 이유와 함께 주석 처리한다. 예: `// [DEPRECATED-TENTATIVE] 자석 정지 반경 계산 - 불 블록과 결합 시 재사용 가능성 있어 보류`.
- 완전한 대체와 폐기가 합의된 코드는 주석으로 남기지 않고 삭제한 뒤, 무엇을 왜 제거했는지 `CHANGELOG.md`에 기록한다.

## 6. PR 및 병합 전 체크리스트

- [ ] 새 TILE id 또는 레벨 번호가 사전 예약과 일치하는가
- [ ] 여러 파일에 걸친 객체 및 switch 변경이 각 파일의 마지막 항목 뒤에 있는가
- [ ] 순서 의존성이 있으면 `[ORDER-DEPENDENT]` 주석이 있는가
- [ ] 새 이동 메커니즘이 독립 메서드로 분리되었는가
- [ ] 손대지 않은 영역에 포맷팅 변경이 섞이지 않았는가
- [ ] 풀이 가능성에 영향을 주는 변경이면 solver 검증 결과가 기록됐는가
- [ ] 완전히 대체된 로직은 삭제되고 `CHANGELOG.md`에 기록됐는가
- [ ] 수정 이유가 주석 또는 커밋 메시지에 남아 있는가
