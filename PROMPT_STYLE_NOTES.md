# 협업 스타일 정리 (이 대화 기준)

Claude Code와의 협업 세션에서 관찰된 사용자 프롬프트 스타일과 반복 패턴 정리.

## 1. 표현 스타일

- 문장부호·주어 생략, 극단적으로 짧은 지시("ㄱㄷ", "ㅇㅇ", "0.25 ㄱㄱ") — 이전 문맥의 변수를 그대로 알고 있다는 전제로 숫자만 던짐
- 반말/축약체지만 요구사항 자체는 항상 명확한 완성 그림을 갖고 있음
- 설명이 말로 잘 안 될 땐 스크린샷 + 구체적 조건 서술로 전환 (예: "골 부분하고 그 아래가 비어있어... 그 비어있는게 곂치는 부분을 아예 배경이 없도록")
- 필요하면 "한국어로 설명해줄래"처럼 언어/톤도 직접 조율

## 2. 반복적으로 나타난 핵심 패턴

**"반대로/좁게/추측으로 구현" → 정확한 근거로 정정**

세션 초반부터 끝까지 일관되게, AI가 코드나 데이터를 확인하지 않고 추측으로 답하거나 요청을 좁게/반대로 해석해서 구현하면, 사용자가 정확한 근거(스크린샷, 좌표, 조건문, 실제 코드 위치)를 제시하며 바로잡는 흐름이 반복됨.

### 초반 사례 (뚫린 공간 배경 노출 기능, 6차례+ 정정)

- "왜 안됐어. 반대로 한거야?" → 실제 원인은 반대 구현이 아니라 데이터에 트리거 조건 자체가 없었던 것 — 원인 진단 실패
- "잉 그니깐 벽인 곳만" → 반대로 구현 → "아니아니 벽일경우 배경 노출 안한다고. 즉, 벽이 아닌 경우에 노출"
- "시점 전환했을 땐 z기준이 아니라 x기준이 되어야지" — 축 하드코딩 오류
- Tab 롱프레스: AskUserQuestion 두 번 답변했음에도 "마우스 위치는 축 슬라이스 선택에 관여 안한다고? 해야되는데... 우리 둘이 이해가 달랐나" — 선택지를 명확히 골라줬는데도 좁게/보수적으로 해석해 기능을 축소 구현

### 최근 사례 (아이템/좌표 관련)

- Z-Y 뷰 미러 설정: localStorage 캐시로 인한 "반영 안 됨" 문제를 사용자가 먼저 원인을 짚음
- "너 zy뷰 몇인지 제대로 확인 안해? 스크린샷에 있는 인게임 화면이 16×9로 보여?" — 사이드바에 표시된 실제 치수(Z9×Y12)를 확인 안 하고 X값(16)을 가로 폭으로 착각
- "너 아이템 적용방식 다시 정독하고 와. 너 더위먹었니" — 아이템 소비/슬라이딩 정지 규칙을 코드 재확인 없이 옛 기억(전면충돌 후 꺾기 등, 실제로는 존재하지 않는 메커니즘)으로 답함
- 퍼즐 배치 설계(9×10 그리드에 얼음 강제 경로)는 결국 "내가 짤게... 너 이런 퍼즐겜에 좀 약하구나"로 직접 처리 — 반복 실패 후 위임을 접음

## 3. 위임 vs 직접 처리의 경계

- **계속 위임**: 코드 위치 특정, 값 변경(수치 튜닝), 렌더링 로직 추가/삭제, 문서(README/CHANGELOG) 갱신, 설정 UI/토글 구현 — 스펙이 명확하고 검증 가능한 작업
- **결국 회수**: 게임 고유의 복잡한 상호작용 로직(슬라이딩 이동 규칙, 축 매핑, 퍼즐 경로 설계)처럼 정밀한 공간 추론이 필요한 작업 — AI가 반복적으로 좌표축/규칙을 헷갈리자 신뢰를 접고 직접 처리

## 한 줄 요약

짧고 압축된 말로 방향만 던지되, 결과물을 실제 코드/화면/데이터와 대조해 어긋난 지점을 정확히 짚어 정정하는 스타일. 가장 반복된 실패 패턴은 **"확인 없이 추측(특히 좌표축, 게임 규칙, 기능 트리거 조건)으로 답하는 것"**이며, 세션 초반(뚫린 공간 배경)부터 최근(아이템 규칙, Z-Y 뷰 치수)까지 동일하게 재발했음 — 단발성 실수가 아니라 "코드/데이터를 실제로 열어보지 않고 답하는" 습관성 문제로 보임.

## 개선 지침

새 기능/버그 답변 전에:
1. 관련 코드를 실제로 Read/Grep해서 현재 동작을 확인한다 (기억이나 이전 세션 지식에 의존하지 않음)
2. 좌표축, 화면 치수 등 수치 정보는 UI에 표시된 값(사이드바, 스크린샷 라벨 등)을 그대로 신뢰하고 재계산하지 않는다
3. 불확실하면 추측해서 구현하기 전에 먼저 코드를 더 찾아보거나 사용자에게 확인한다
