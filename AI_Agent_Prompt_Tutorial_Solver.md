# 구현 프롬프트: 튜토리얼 레벨 + solver 기반 검증 워크플로우

`blob-escape` 프로젝트의 튜토리얼 레벨을 만드는 작업이다. 맵을 손으로 짤 때마다 계속 틀리고 있으니, 앞으로는 **절대 맵을 만들고 바로 완료 처리하지 말고** 아래 순서를 반드시 지켜서 진행한다.

## 왜 이 워크플로우가 필요한가

이 게임의 이동 규칙(막힐 때까지 미끄러짐 + 자석 스냅 + 고무 튕김 + 불 블록 경로 전체 차단, 3D 레이어까지 겹친 그리드)은 "이 스와이프를 하면 정확히 어디서 멈추는가"를 눈대중/추론만으로 정확히 맞히기 매우 어렵다. 실제로 팀에서 33~37번 레벨을 만들 때도 손검증 대신 BFS solver로 검증했다는 기록(`CHANGELOG.md`)이 있지만, 그 solver 자체는 저장소에 파일로 남아있지 않다. 검증 도구 없이 맵을 짜면 틀릴 수밖에 없으므로, **먼저 검증 도구를 만들고, 그 도구로 매번 확인하면서 맵을 완성**하는 방식으로 바꾼다.

---

## 1단계 — 순수 로직 시뮬레이터 작성

렌더링/사운드 없이 게임의 이동 규칙만 그대로 복제한 Node.js 스크립트를 `blob-escape/tools/solver.js`에 새로 만든다.

- `blob-escape/js/game.js`의 `_resolveSlide`, `_scanForFire`, 자석 스냅 로직, 고무벽 튕김 로직을 그대로 옮겨오되 캔버스/DOM 의존성은 전부 제거한다.
- 레벨 JSON(`blob-escape/levels/level_XX.json`)을 직접 읽어서 동작해야 한다.
- 상태 공간은 `(x, y, z, playerState, 소비한 아이템 집합, 연 문 집합, 소화한 불 블록 집합)`으로 정의하고, 이 상태들 사이를 BFS로 탐색해서 시작 지점(`P`)에서 포탈(`X`)까지 도달 가능한지 판정하는 함수를 구현한다.
- 매 상태에서 현재 뷰 평면 기준 4방향 스와이프를 시도해서 다음 상태로 전이하는 식으로 구현한다.

## 2단계 — 튜토리얼 전용 검증 함수 추가

단순 "풀 수 있는가"만으로는 부족하다. 아래 함수를 solver에 추가한다.

- `assertBlockedDirectly(level, itemState)` — 특정 상태(예: `NORMAL`)일 때 시작 지점에서 특정 방향으로 스와이프하면 이동량이 0인지(그 방향 자체가 막혀 있는지) 확인.
- `assertRequiresItem(level, itemType)` — 그 아이템을 제거하면(또는 아이템을 안 먹은 상태로 고정하면) 포탈에 도달 불가능해지는지 확인. 33~37번 레벨에 쓰였던 방식과 동일한 개념.
- `assertSolvedWithinMoves(level, startState, maxMoves)` — 정답 경로가 특정 이동 횟수 이하로 존재하는지 확인. 튜토리얼은 정답 경로가 짧고 명확해야 하므로, 이걸로 "너무 복잡한 맵"이 되는 걸 막는다.

이 함수들은 `true`/`false`뿐 아니라 실패 이유(예: "3번 방향 스와이프가 막혀야 하는데 실제로는 2칸 이동함")도 함께 출력해야 한다. CLI에서 `node tools/solver.js levels/level_XX.json` 형태로 실행하면 해당 레벨의 검증 결과가 콘솔에 출력되도록 만든다.

## 3단계 — 튜토리얼 스테이지 제작

**맵을 만들 때마다 반드시 solver로 검증부터 하고, 통과할 때까지 맵을 고쳐서 다시 검증한다. 검증을 통과하지 못한 맵은 완료로 보고하지 않는다.**

1. **이동 튜토리얼** — 아이템/장애물 없음. 상하좌우 이동 감각만 익히는 맵.
2. **자석 + 철 벽** (한 스테이지에 통합) — 철 벽 근처를 지나면 자석 상태로 그 자리에 붙는다는 것을 학습.
3. **불 블록 단독** — 아이템 없음. `assertBlockedDirectly`로, 포탈로 가는 직선 경로에 불 블록이 있어서 그 방향 자체가 막혀 있는지 확인하고, 반드시 우회 경로로만 포탈에 도달 가능하게 만든다.
4. **불 블록 + 얼음 아이템** — `assertRequiresItem`으로 얼음 없이는 클리어 불가능한지 확인. 얼음을 먹자마자 아까 막혔던 방향으로 바로 진입하면 짧게 클리어되도록, `assertSolvedWithinMoves`로 이 경로가 몇 수 이내인지 확인한다.
5. **전기 문 단독** — 불 블록 단독과 같은 패턴(문이 막혀 있어 우회 필요). *이 부분은 확실히 확인된 가정이 아니니, 진행 전에 다시 물어봐줄 것.*
6. **전기 문 + 전기 아이템** — 얼음+불 블록과 같은 패턴.

각 맵마다: 맵 JSON 작성 → `node tools/solver.js`로 검증 → 실패하면 콘솔 출력을 보고 맵 수정 → 다시 검증. 통과할 때까지 반복한다.

---

## 레벨 JSON 포맷 참고

```json
{
  "id": 1,
  "name": "이름",
  "subtitle": "Moves required: N",
  "map": [
    ["#############", "#.P........X#", "#############"]
  ]
}
```

`map`은 레이어(z) 배열이고, 각 레이어는 행(y) 문자열 배열이다. 문자 매핑은 `blob-escape/js/constants.js`의 `CHAR_TO_TILE`을 그대로 따른다: `#`/공백=벽, `M`=철벽, `R`=고무벽, `D`=전기문, `X`=포탈, `P`=시작지점, `m`/`i`/`e`=자석/얼음/전기 아이템. 불 블록 문자(예: `F`)가 아직 `CHAR_TO_TILE`에 없으면 먼저 추가하고 진행한다.
